@frontend @ui @email-editor
Feature: 郵件編輯器沉浸式版面
  # Issue #258 — 郵件編輯器編輯區被左右遮蔽
  # 對應 Activity: 郵件編輯器沉浸式版面.mmd
  # 對應 UI Spec: specs/ui/郵件模板編輯器版面.md
  #
  # 郵件模板編輯器使用 j7-easy-email-extensions StandardLayout（左：元件區 / 中：信件畫布 / 右：屬性區）。
  # 該三欄佈局原為整頁全螢幕設計，嵌入 WordPress 後台後外層疊著 WP 主選單 + Refine 側邊欄，
  # 導致中間信件畫布被左右擠壓、需反覆左右捲動。
  # 本 Feature 描述沉浸式版面優化後的前端呈現行為（純 UI，不涉及 API 與資料模型）。
  # 由 Playwright E2E 驗證。

  Background:
    Given 管理員 "Admin" 已登入後台
    And 系統中存在郵件模板 400「課程開通通知」

  # ========== 沉浸模式：自動進入 ==========

  Rule: 進入郵件模板編輯頁時自動進入沉浸全螢幕模式

    Scenario: 開啟編輯頁自動隱藏後台側邊欄，三欄編輯器獨佔瀏覽器寬度
      When 管理員 "Admin" 開啟郵件模板 400 的編輯頁
      Then 進入沉浸全螢幕模式
      And WordPress 後台主選單被隱藏，不佔用畫面寬度
      And Refine 側邊欄被隱藏，不佔用畫面寬度
      And 左側元件區、中間信件畫布、右側屬性區三欄佔滿瀏覽器可視寬度
      And 中間信件畫布完整顯示信件版面，無需左右捲動即可看見全貌

  # ========== 沉浸模式：切換與偏好記憶 ==========

  Rule: 管理員可在沉浸模式與一般後台版面之間自由切換

    Scenario: 於沉浸模式點擊退出全螢幕返回一般後台版面
      Given 管理員 "Admin" 正處於郵件編輯頁的沉浸全螢幕模式
      When 管理員 "Admin" 點擊「退出全螢幕」按鈕
      Then 退出沉浸模式，回到一般後台版面
      And WordPress 後台主選單、頂部工具列、頁尾儲存列恢復正常顯示與可操作
      And 畫面上出現「進入全螢幕編輯」按鈕

    Scenario: 於一般版面點擊進入全螢幕再次進入沉浸模式
      Given 管理員 "Admin" 位於郵件編輯頁的一般後台版面
      When 管理員 "Admin" 點擊「進入全螢幕編輯」按鈕
      Then 重新進入沉浸全螢幕模式
      And 三欄編輯器獨佔瀏覽器可視寬度

  Rule: 系統記住管理員上次選擇的版面模式（偏好記憶，client-side 持久化）

    Scenario: 上次退出沉浸模式後，下次進入編輯頁沿用一般版面
      Given 管理員 "Admin" 上次在編輯頁選擇了「一般後台版面」
      When 管理員 "Admin" 再次開啟任一郵件模板的編輯頁
      Then 依上次偏好維持一般後台版面，不自動進入沉浸模式

    Scenario: 上次維持沉浸模式，下次進入編輯頁沿用沉浸全螢幕
      Given 管理員 "Admin" 上次在編輯頁維持「沉浸全螢幕模式」
      When 管理員 "Admin" 再次開啟任一郵件模板的編輯頁
      Then 依上次偏好自動進入沉浸全螢幕模式

  # ========== 面板收合 ==========

  Rule: 左側元件區與右側屬性區可各自收合，收合後空間讓給中間畫布

    Scenario: 收合左側元件區後中間畫布變寬
      Given 管理員 "Admin" 正處於沉浸全螢幕模式
      When 管理員 "Admin" 收合左側元件區
      Then 左側元件區收起
      And 中間信件畫布的可視寬度隨之增加
      And 左側元件區可再次展開叫回

    Scenario: 收合右側屬性區後中間畫布變寬
      Given 管理員 "Admin" 正處於沉浸全螢幕模式
      When 管理員 "Admin" 收合右側屬性區
      Then 右側屬性區收起
      And 中間信件畫布的可視寬度隨之增加
      And 右側屬性區可再次展開叫回

  # ========== 中間畫布最小寬度 ==========

  Rule: 中間信件畫布可視寬度不得低於 640px（信件標準 600px + 左右內距）

    Scenario Outline: 各斷點下中間畫布維持足夠寬度，信件全貌一眼可見
      Given 瀏覽器視窗寬度為 <viewport>px
      When 管理員 "Admin" 開啟郵件模板 400 的編輯頁
      Then 中間信件畫布可視寬度不低於 640px
      And 無需左右捲動即可看見信件（600px）完整版面
      And 左側元件區、中間畫布、右側屬性區三者互不重疊遮蔽

      Examples:
        | 斷點      | viewport |
        | 13 吋筆電 | 1280     |
        | 14/15 吋  | 1440     |
        | 桌機      | 1920     |
        | 超寬螢幕  | 2560     |

  # ========== 小螢幕浮動抽屜 ==========

  Rule: 螢幕過窄致三欄同時展開會擠壓畫布時，左右面板改為浮動抽屜，不佔畫布寬度

    Scenario: 窄螢幕下左右面板以浮動抽屜呈現，優先保住畫布寬度
      Given 瀏覽器視窗寬度為 1280px 且展開三欄會使中間畫布不足 640px
      When 管理員 "Admin" 開啟郵件模板 400 的編輯頁
      Then 左右面板平常收起為浮動抽屜，不佔用中間畫布寬度
      And 中間信件畫布維持不低於 640px 的可視寬度

    Scenario: 窄螢幕下點按鈕滑出浮動抽屜，覆蓋於畫布上方
      Given 管理員 "Admin" 在窄螢幕編輯頁，左側元件區為收起的浮動抽屜
      When 管理員 "Admin" 點擊左側元件區的展開按鈕
      Then 左側元件區以浮動抽屜滑出，覆蓋在畫布上方，不推擠畫布寬度
      And 管理員可從浮動抽屜拖入元件
      And 再次點擊即收起浮動抽屜

  # ========== 不影響後台既有元件 ==========

  Rule: 版面優化不得破壞 WordPress 後台既有選單、工具列與儲存列

    Scenario: 退出沉浸模式後後台既有元件完整可用
      Given 管理員 "Admin" 正處於沉浸全螢幕模式
      When 管理員 "Admin" 退出沉浸模式
      Then WordPress 後台主選單完整顯示且可點擊導覽
      And 頂部工具列正常顯示
      And 頁尾儲存列與「儲存郵件」按鈕正常顯示且可點擊

  # ========== 既有信件內容不受影響 ==========

  Rule: 版面優化僅改變編輯畫面呈現，不改變任何已儲存信件內容與寄出排版

    Scenario: 沉浸模式下編輯並儲存，信件內容與寄出排版維持一致
      Given 郵件模板 400 已有既存信件內容
      When 管理員 "Admin" 在沉浸模式下開啟郵件模板 400 但未更動內容
      And 管理員 "Admin" 儲存郵件模板 400
      Then 郵件模板 400 的信件內容維持不變
      And 寄出的信件排版與優化前一致
